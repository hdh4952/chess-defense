# 체스 디펜스 (가제) — 게임 명세서 v1.2

> 모든 항목이 확정된 완성본입니다. 이 문서만으로 구현에 착수할 수 있습니다.
> v1.0 구현 완료 이후 사용자 요청으로 승인된 두 가지 게임 규칙 변경(나이트 쿨다운 폐지, 점유 칸
> 맞교환)이 v1.1로 반영돼 있고, v1.2에서는 공격 사운드(발사음 3종 + 나이트 블라스트음)가 첫
> 슬라이스로 추가됐습니다. 변경 이력은 문서 최하단에 있습니다.

---

## 1. 게임 개요

| 항목 | 내용 |
|---|---|
| 장르 | 타워 디펜스 (실시간 재배치형) |
| 핵심 컨셉 | 체스판과 체스 기물의 행마법을 공격 패턴으로 재해석한 디펜스 게임 |
| 차별 요소 | **웨이브 진행 중에도 기물을 자유롭게 옮겨 적의 흐름에 대응하는 실시간 운영** |
| 플레이 필드 | 8×8 체스판 |
| 플랫폼 | 웹 브라우저 (PC) |
| 총 웨이브 | 20 웨이브 |
| 승리 조건 | 20웨이브 클리어 |
| 패배 조건 | 플레이어 체력 0 |
| 핵심 루프 | 적 처치 → 골드 획득 → 상점에서 기물 구매 → 배치 및 실시간 재배치 → 다음 웨이브 |

---

## 2. 게임 보드 / 좌표계

- 보드 크기: **8×8 (64칸)**
- 좌표 표기: 파일(열) `a~h`, 랭크(행) `1~8`
- 적 스폰 지역: **8랭크** / 적 목표 지점: **1랭크**
- 적 진행 방향: 8랭크 → 1랭크 (위 → 아래)

### 2.1 배치 규칙

| 규칙 | 내용 |
|---|---|
| 배치 가능 영역 | **1~7랭크, 총 56칸** (1랭크 포함) |
| 배치 불가 영역 | **8랭크** (적 스폰 구역) |
| 칸당 기물 수 | **1개** |
| 배치 방법 | 좌측 슬롯의 기물을 보드로 드래그 앤 드롭 |

### 2.2 칸 판정 기준
적은 픽셀 단위로 연속 이동하므로 **적의 중심 좌표가 속한 칸**을 현재 칸으로 판정한다. 모든 공격 범위 판정은 이 기준을 따른다.

---

## 3. 게임 흐름

```
게임 시작 (골드 300, 기물 0개, 체력 30)
   ↓
[준비 시간 10초] ─ 수동 시작 가능, 보너스 없음
   │  ※ 보스 웨이브 직전에는 경고 표시
   ↓
[웨이브 시작] 8랭크의 임의 파일에서 1초 간격 스폰
   ↓
적이 같은 파일로 1랭크를 향해 직진 (일반 3초/칸, 보스 6초/칸)
   │  ※ 이 동안에도 플레이어는 기물을 자유롭게 재배치할 수 있다
   ├─ 처치 → 골드 획득 (= 적의 최대 체력)
   └─ 1랭크 통과 → 소멸 + 체력 감소 (일반 −1 / 보스 −5)
   ↓
[웨이브 클리어] 보너스 골드 +300
   ↓
[준비 시간 10초] → 다음 웨이브 ... 반복
   ↓
20웨이브 클리어 → 승리     |     체력 0 → 즉시 게임 오버
```

### 3.1 종료 처리

| 상황 | 처리 |
|---|---|
| 플레이어 체력 0 | **즉시 게임 정지.** 그 자리에서 멈춘 뒤 결과 화면 표시 |
| 20웨이브 보스 **처치** | 즉시 **승리 화면**으로 전환 |
| 20웨이브 보스가 **1랭크 도달** (체력 6 이상 잔존) | 보스 소멸 + 체력 −5 → **웨이브 클리어 처리 → 승리 화면** |

> 보스는 다른 적과 동일하게 1랭크 도달 시 소멸하며 웨이브가 종료된다. 즉 "보스를 놓쳐도 살아남기만 하면 클리어"다. 단 보스 처치 보상(최대 체력만큼의 골드)은 받지 못한다.

### 3.2 결과 화면

| 표시 항목 | 설명 |
|---|---|
| 도달 웨이브 | 최종 도달 웨이브 번호 |
| 처치 수 | 게임 전체 누적 처치 마릿수 |
| 획득 골드 | 게임 전체 누적 획득 골드 (처치 보상 + 웨이브 보너스) |

---

## 4. 적(몹) 시스템

### 4.1 일반 적

| 항목 | 값 |
|---|---|
| 적 종류 | **단일 타입** (추후 확장) |
| 외형 | **체스 폰(♟) — 블랙** |
| 스폰 위치 | **8랭크의 임의 파일** (a~h 랜덤) |
| 이동 방식 | **실시간 연속 이동 (픽셀 단위)** |
| 이동 경로 | **스폰된 파일 그대로 직진** |
| 이동 속도 | **1칸당 3초** (보드 종주 24초) |
| 체력 | 4.3 공식 참조 |
| 기물과의 상호작용 | **그대로 통과** (충돌·차단 없음) |
| 적의 공격 | **없음** |
| 처치 보상 | **최대 체력과 동일한 골드** |
| 1랭크 도달 시 | 소멸 + 플레이어 체력 **−1** |
| 체력바 | **상시 표시** |

### 4.2 보스

| 항목 | 값 |
|---|---|
| 등장 웨이브 | **5의 배수 웨이브** (5, 10, 15, 20) |
| 외형 | **체스 킹(♚) — 블랙**, 크기는 일반 적과 동일 |
| 동반 적 | **없음** — 보스 1마리만 등장 |
| 체력 | **해당 웨이브 일반 적 체력 × 30** |
| 이동 속도 | **1칸당 6초** (일반 적의 1/2, 종주 48초) |
| 1랭크 도달 시 | 소멸 + 플레이어 체력 **−5**, 웨이브는 클리어 처리 |
| 처치 보상 | **최대 체력과 동일** |
| 체력바 | **상시 표시** (비율 기반) |
| 등장 연출 | **경고 표시** (7.9 참조) |

### 4.3 체력 공식

```
웨이브 1~10  : 체력 = 10 + (웨이브 − 1)       // +1씩 증가
웨이브 11~20 : 체력 = 19 + 3 × (웨이브 − 10)  // +3씩 증가
보스          : 위 값 × 30
```

### 4.4 웨이브 구조

| 항목 | 값 |
|---|---|
| 방식 | **웨이브제**, 총 20웨이브 |
| 웨이브당 적 수 | **10 + 2×(웨이브−1)** (보스 웨이브 제외) |
| 스폰 간격 | **1.0초** |
| 준비 시간 | **10초** |
| 수동 시작 | **가능** (보너스 없음) |
| 클리어 보너스 | **+300골드** |
| 종료 판정 | 해당 웨이브의 모든 적이 사망 또는 1랭크 도달 |

### 4.5 웨이브 데이터 테이블

| 웨이브 | 적 수 | 개체 체력 | 총 체력 | 스폰 소요 | 전멸 보상 | 누적 자금 상한 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 10 | 10 | 100 | 10.0s | 100G | 700G |
| 2 | 12 | 11 | 132 | 12.0s | 132G | 1,132G |
| 3 | 14 | 12 | 168 | 14.0s | 168G | 1,600G |
| 4 | 16 | 13 | 208 | 16.0s | 208G | 2,108G |
| **5** | **1 (보스)** | **420** | 420 | — | 420G | 2,828G |
| 6 | 20 | 15 | 300 | 20.0s | 300G | 3,428G |
| 7 | 22 | 16 | 352 | 22.0s | 352G | 4,080G |
| 8 | 24 | 17 | 408 | 24.0s | 408G | 4,788G |
| 9 | 26 | 18 | 468 | 26.0s | 468G | 5,556G |
| **10** | **1 (보스)** | **570** | 570 | — | 570G | 6,426G |
| 11 | 30 | 22 | 660 | 30.0s | 660G | 7,386G |
| 12 | 32 | 25 | 800 | 32.0s | 800G | 8,486G |
| 13 | 34 | 28 | 952 | 34.0s | 952G | 9,738G |
| 14 | 36 | 31 | 1,116 | 36.0s | 1,116G | 11,154G |
| **15** | **1 (보스)** | **1,020** | 1,020 | — | 1,020G | 12,474G |
| 16 | 40 | 37 | 1,480 | 40.0s | 1,480G | 14,254G |
| 17 | 42 | 40 | 1,680 | 42.0s | 1,680G | 16,234G |
| 18 | 44 | 43 | 1,892 | 44.0s | 1,892G | 18,426G |
| 19 | 46 | 46 | 2,116 | 46.0s | 2,116G | 20,842G |
| **20** | **1 (보스)** | **1,470** | 1,470 | — | 1,470G | 22,612G |
| **합계** | **452** | — | **16,312** | — | **16,312G** | — |

**누수 허용치**: 시작 체력 30, 회복 없음 → 게임 전체에서 **일반 적 29마리**까지 통과 가능 (보스 1마리 = 일반 5마리분)

---

## 5. 기물 시스템

### 5.1 공통 규칙

| 항목 | 내용 |
|---|---|
| 조작 | 배치 후 **자동 공격** (나이트만 예외) |
| 배치 영역 | 1~7랭크 (8랭크 불가) |
| **이동** | **웨이브 진행 중에도 무제한 자유 이동** — 쿨다운·횟수 제한 없음 (나이트만 예외) |
| **이동 목적지가 점유된 칸일 때 (v1.1)** | **보드 → 보드 이동에 한해 두 기물이 자리를 맞바꾼다.** 트레이 → 보드 배치는 여전히 빈 칸만 허용(밀려날 상대가 없으므로). 양쪽 기물의 쿨다운은 각자 그대로 유지되며, 퀸 버프는 맞교환 직후 한 번 재계산된다 |
| **이동 시 쿨다운** | **초기화되지 않고 그대로 유지** — 이동해도 진행 중이던 공격 쿨다운은 계속 흐른다 |
| 회수 | **가능** — 보드 → 좌측 슬롯으로 드래그 |
| 판매 | **가능** — 우측 판매 슬롯으로 드래그, **구매가의 50%** 환급, 확인창 없음 |
| 파괴 | 적이 기물을 공격하지 않으므로 파괴되지 않음 |
| 다중 타격 | **한 칸에 적이 여러 마리 있으면 전부 타격** (모든 기물 공통) |
| **쿨다운 관리 기준** | **기물 ID 기준으로 상태를 유지한다.** 보드 ↔ 슬롯을 오가도 해당 기물의 쿨다운은 초기화되지 않는다 |
| 외형 | **화이트** (적은 블랙) |

> **실시간 재배치가 이 게임의 핵심 재미입니다.** 적이 몰리는 파일을 읽고 기물을 옮겨 대응하는 것이 주된 플레이 행위이며, 밸런스도 이를 전제로 설계합니다.
>
> 쿨다운이 이동으로 초기화되지 않고 기물 ID로 유지되므로, 폰·비숍·룩은 **회수→재배치나 잦은 이동으로 쿨다운을 우회하는 플레이가 불가능합니다.** **나이트는 예외입니다 (v1.1)** — 이동 쿨다운 자체가 폐지되어(5.3 참조) 애초에 우회할 쿨다운이 없으므로, 이 안티파밍 조항은 나이트에는 더 이상 적용되지 않습니다. 이는 사용자가 명시적으로 요청하고 결과를 승인한 의도된 변경입니다.

### 5.2 폰 (Pawn)

| 항목 | 값 |
|---|---|
| 가격 | **100골드** / 판매가 50골드 |
| 공격 범위 | **전방 대각선 2방향, 각 1칸** (↖ ↗ — 8랭크 방향) |
| 데미지 | **2** |
| 공격 주기 | **0.5초** (DPS 4.0) |
| 다중 대상 | 양쪽 대각선 동시 타격, 각 칸의 모든 적 타격 |
| 이펙트 | **노말 속성** |

> 폰 1개가 **2개 파일**을 커버한다. `(f, r)`의 폰은 `(f−1, r+1)`과 `(f+1, r+1)`을 공격한다.

### 5.3 나이트 (Knight)

| 항목 | 값 |
|---|---|
| 가격 | **300골드** / 판매가 150골드 |
| 공격 범위 | **자신의 칸 + 주변 8방향 = 총 9칸** |
| 데미지 | **3** (범위 내 모든 적) |
| 발동 조건 | **배치 시 1회** + **이동 완료 시 1회** (단발성) |
| 이동 방식 | **플레이어가 직접 드래그** |
| 이동 쿨다운 | **없음 (v1.1)** — 배치·이동마다 매번 폭발한다. (v1.0에서는 3초, 최초 배치 시점부터 카운트 시작이었으나 사용자 요청으로 완전히 폐지) |
| 쿨다운 유지 | **기물 ID 기준.** 슬롯으로 회수했다가 재배치해도 쿨다운은 이어진다. (메커니즘 자체는 v1.1에도 그대로 남아 있고 config 값만 0이 됐다 — 값을 되돌리면 이 규칙이 다시 살아난다) |
| 이펙트 | **불 속성** |

**이동 제약**
- 나이트 행마법(L자)으로 도달 가능한 칸으로만 이동
- **8랭크 이동 불가**
- **점유된 칸으로도 이동 가능 (v1.1)** — 목적지에 다른 기물이 있으면 실패하지 않고 그 기물과 자리를 맞바꾼다(5.1 공통 규칙 참조). 맞교환으로 밀려난 기물은 그 자신이 선택한 이동이 아니므로, 나이트여도 폭발하지 않는다 — 폭발은 플레이어가 직접 옮긴 나이트 쪽에서만 새 자리 기준으로 일어난다
- 쿨다운 중 드래그 시도 시 원위치 복귀, UI에 남은 쿨다운 표시 (게이트 자체는 유지되지만 v1.1 기준 쿨다운이 항상 0이라 실질적으로 발생하지 않는다)

### 5.4 비숍 (Bishop)

| 항목 | 값 |
|---|---|
| 가격 | **300골드** / 판매가 150골드 |
| 공격 범위 | **자신의 칸 포함, 4방향 대각선 전체** |
| 사거리 | **무제한** |
| 데미지 | **3** |
| 공격 주기 | **3초** |
| 관통 | 기물·적 모두 막지 않음 (완전 관통) |
| 이펙트 | **빛 속성** |

> 대각선이 거의 모든 파일을 가로지르므로 사실상 **보드 전역 광역 딜러**. 중앙(d4/e4/d5/e5) 배치 시 효율 최대.

### 5.5 룩 (Rook)

| 항목 | 값 |
|---|---|
| 가격 | **500골드** / 판매가 250골드 |
| 공격 범위 | **자신의 칸 포함, 상하좌우 4방향 직선 전체** |
| 사거리 | **무제한** |
| 데미지 | **5** |
| 공격 주기 | **3초** |
| 관통 | 기물·적 모두 막지 않음 (완전 관통) |
| 이펙트 | **땅 속성** |

> 자기 파일의 적에게는 종주 내내 8회(총 40 피해), 다른 파일 적에게는 1회(5 피해). **후반 일반 웨이브의 주력.**

### 5.6 퀸 (Queen)

| 항목 | 값 |
|---|---|
| 가격 | **900골드** / 판매가 450골드 |
| 자체 공격 | **없음** (순수 서포터) |
| 버프 범위 | 상하좌우 + 대각선 **8방향 직선 전체** (자신 칸 포함) |
| 버프 효과 | 범위 내 아군 기물의 **기본 공격력 ×2** |
| 차단 | 다른 기물이 버프 경로를 막지 않음 |
| 이펙트 | 8방향 상시 오라 + 버프 대상 발밑 링 |

```
최종 공격력 = 기본 공격력 × (1 + 자신을 비추는 퀸의 수)
```

| 겹친 퀸 수 | 0 | 1 | 2 | 3 |
|---|---|---|---|---|
| 배율 | ×1 | ×2 | ×3 | ×4 |

- 퀸은 자기 자신에게도 버프를 적용하나 자체 공격이 없어 실효 없음
- **모든 기물의 이동 시 버프가 실시간으로 재계산**된다
- 나이트의 데미지는 **폭발 발생 시점**의 버프 상태로 계산

### 5.7 기물 요약표

| 기물 | 가격 | 판매가 | 데미지 | 주기 | 커버 범위 | 이펙트 | 역할 |
|---|---:|---:|---:|---|---|---|---|
| 폰 | 100G | 50G | 2 | 0.5초 | 전방 대각선 1칸 ×2 (2개 파일) | 노말 | 초반 주력 · 보스 추격 |
| 나이트 | 300G | 150G | 3 | 이동 시마다 (쿨다운 없음, v1.1) | 주변 9칸 | 불 | 수동 조작 광역 버스트 — 화력이 입력 속도로만 제한됨 |
| 비숍 | 300G | 150G | 3 | 3초 | 대각선 4방향 전체 | 빛 | 전역 광역 딜러 |
| 룩 | 500G | 250G | 5 | 3초 | 직선 4방향 전체 | 땅 | 단일 파일 전담 킬러 |
| 퀸 | 900G | 450G | — | — | 8방향 전체 | 오라 | 공격력 ×2 버퍼 |

---

## 6. 경제 시스템

| 항목 | 값 |
|---|---|
| 재화 단위 | **골드 (G)** |
| 시작 골드 | **300G** |
| 시작 기물 | **없음** |
| 적 처치 보상 | **적의 최대 체력과 동일** (보스 포함) |
| 웨이브 클리어 보너스 | **+300G** |
| 자동 수입 / 이자 | **없음** |
| 기물 판매 환급률 | **구매가의 50%** |

**전 게임 이론 최대 수입**: 처치 보상 16,312G + 웨이브 보너스 6,000G + 시작 300G = **22,612G**

---

## 7. UI / UX

### 7.1 화면 레이아웃

```
┌─────────────────────────────────────────────────────────────────┐
│ ♥30 │ 💰300 │ 웨이브 1/20 │ 남은 적 10 │ ⏱10s │ ⏸ │ ▶▶2x        │
├──────────────┬──────────────────────────────┬───────────────────┤
│  기물 슬롯   │        체스판 (8×8)          │    판매 슬롯      │
│    4×4       │                              │        🗑          │
│   (16칸)     │   ↓ 8랭크 (적 스폰, 배치 X)  │                   │
│  ┌─┬─┬─┬─┐   │                              │  드래그 = 즉시    │
│  ├─┼─┼─┼─┤   │        ...                   │  판매 (50%)       │
│  ├─┼─┼─┼─┤   │                              │  확인창 없음      │
│  └─┴─┴─┴─┘   │   ↓ 1랭크 (방어선, 배치 O)   │                   │
├──────────────┤                              ├───────────────────┤
│    상점      │                              │  [ 웨이브 시작 ]  │
│ ♟100 ♞300    │                              │                   │
│ ♝300 ♜500    │                              │                   │
│ ♛900         │                              │                   │
└──────────────┴──────────────────────────────┴───────────────────┘
```

### 7.2 기물 슬롯

| 항목 | 내용 |
|---|---|
| 위치 | 화면 **좌측** / 구조 **4×4 = 16칸** |
| 스택 | **없음** — 동일 기물도 1개당 1칸 차지 |
| 가득 찼을 때 | **구매 불가** (상점 버튼 비활성화) |
| 내부 재정렬 | **가능** |
| 보드에서 회수 | **가능** (쿨다운은 기물 ID 기준으로 유지) |

### 7.3 판매 슬롯

| 항목 | 내용 |
|---|---|
| 위치 | 화면 **우측** |
| 환급 | 구매가의 **50%** |
| 대상 | 보드 위 / 슬롯 내 기물 모두 |
| 확인창 | **없음** — 드롭 직전 hover 시 환급액 프리뷰 표시 권장 |

### 7.4 상점

| 항목 | 내용 |
|---|---|
| 판매 품목 | **5종 상시 판매** |
| 오픈 시점 | **상시** (웨이브 진행 중에도 구매 가능) |
| 랜덤 / 리롤 | **없음** |
| 비활성 조건 | 골드 부족 / 슬롯 만석 / **일시정지 중** |

### 7.5 드래그 앤 드롭 동작표

| 출발 | 도착 | 결과 |
|---|---|---|
| 상점 | (클릭) | 슬롯 빈칸에 기물 추가 |
| 슬롯 | 보드 빈칸 | 배치 |
| 슬롯 | 슬롯 | 순서 재정렬 |
| 슬롯 | 판매 슬롯 | 판매 (50%) |
| **보드** | **보드 빈칸** | **이동 — 웨이브 중에도 자유롭게 가능, 쿨다운 유지** (나이트만 행마법 + 쿨다운 제약) |
| **보드** | **보드 점유 칸 (v1.1)** | **맞교환 — 두 기물이 서로 자리를 바꾼다.** 양쪽 쿨다운 모두 각자 유지되고, 퀸 버프는 맞교환 후 한 번 재계산된다. 나이트가 옮긴 쪽이면 새 자리에서 폭발하지만, 맞교환으로 밀려난 기물은(나이트여도) 폭발하지 않는다 |
| 보드 | 슬롯 빈칸 | 회수 |
| 보드 | 판매 슬롯 | 판매 (50%) |
| 무효 동작 (예: 트레이 → 8랭크, **트레이 → 점유 칸**, 나이트 쿨다운 중 이동, 나이트 L자 아닌 이동) | — | 원위치 복귀 |

> 재배치가 핵심 조작이므로 **드래그 반응 속도와 스냅 정확도**가 게임 완성도를 좌우한다. 드래그 중 목표 칸을 명확히 하이라이트하고, **클릭-투-무브**(출발 칸 클릭 → 도착 칸 클릭) 방식도 함께 지원할 것을 권장한다.

### 7.6 HUD
- 플레이어 체력(30 기준) / 보유 골드 / 현재 웨이브(n/20) / 남은 적 수
- 준비 시간 카운트다운, 웨이브 수동 시작 버튼
- 일시정지 버튼, 배속(1x / 2x) 토글
- 음소거 토글 (**v1.2**) — 공격 사운드(10.1 참조) 전체를 켜고 끈다. 세이브 시스템이 없으므로
  새로고침하면 초기화된다(메모리 상태만 유지, 영속화하지 않음)

### 7.7 편의 기능

| 기능 | 설명 |
|---|---|
| **사거리 미리보기** | 드래그 중 마우스가 올라간 칸의 공격 범위 하이라이트. 나이트는 이동 가능 칸도 표시 |
| **퀸 버프 범위 시각화** | 퀸 선택/드래그 시 8방향 라인 표시. 버프 받는 기물에 상시 표식(배율 숫자) |
| **기물 툴팁** | 기물명 / 기본 공격력 / 버프 배율 / 최종 공격력 / 공격 주기 / 남은 쿨다운 / 판매가 |
| **일시정지** | 게임 정지. **일시정지 중 기물 구매·배치·이동·판매 전부 불가** |
| **배속 2x** | 적 이동·기물 쿨다운·**준비 시간까지 모두 2배** |
| **자동 일시정지** | 브라우저 탭 비활성화 시 자동 일시정지 (`visibilitychange`) |

### 7.8 적 렌더링 — 같은 파일 겹침 처리

스폰 간격 1초 ÷ 칸당 3초 = **적들이 항상 1/3칸 간격으로 줄지어 이동**한다. 완전히 겹치는 경우는 구조적으로 발생하지 않는다.

| 항목 | 권장값 | 이유 |
|---|---|---|
| 적 스프라이트 크기 | 칸의 **50~60%** (80px 칸 기준 40~48px) | 1/3칸(약 27px) 간격에서도 개체 구분 가능 |
| 체력바 | 스프라이트 바로 위, 높이 3~4px | 겹쳐도 개체 수 파악 가능 |
| 가로 지터 | 적 ID 기반 결정론적 **±8px** | 완전 일직선보다 구분이 쉬움 |
| 그리기 순서 | 1랭크에 가까운 적을 나중에(위에) | 선두 적의 위협도 파악 용이 |

> 지터는 **렌더링 전용**이며, 데미지 판정은 항상 논리 좌표 기준으로 처리한다.

### 7.9 보스 경고 표시

보스 웨이브에는 **등장 경고**를 표시한다.

| 시점 | 표시 내용 |
|---|---|
| 보스 웨이브 **준비 시간 시작 시** | 화면 중앙에 "⚠ BOSS WAVE" 배너 (2초간 표시 후 페이드), 준비 시간 UI에 보스 아이콘 상시 표기 |
| 보스 **스폰 순간** | 스폰된 파일 상단에 경고 마커, 해당 파일 전체를 붉은 톤으로 1초간 강조 |
| 보스 **1랭크 접근 시** (2랭크 진입) | 화면 가장자리 붉은 비네트 + 체력바 강조 |

> 보스가 어느 파일에 나오는지 즉시 알려주는 것이 중요하다. 보스는 1마리뿐이라 놓치면 그대로 −5이고, 플레이어가 기물을 그 파일로 옮길 시간을 확보해 줘야 하기 때문이다.

---

## 8. 아트 / 연출 방향

### 8.1 진영 구분

| 진영 | 색상 | 외형 |
|---|---|---|
| **플레이어 기물** | **화이트** | 체스 기물 5종 (폰·나이트·비숍·룩·퀸) |
| **일반 적** | **블랙** | 체스 폰 (♟) |
| **보스** | **블랙** | 체스 킹 (♚), 크기는 동일 |

> 플레이어도 폰을 사용하므로 **아군 폰(화이트)과 적 폰(블랙)의 구분이 가장 중요하다.** 색상 외에도 다음 요소로 다층 구분할 것을 권장한다.
> - 적은 **체력바가 있고**, 아군 기물은 없다
> - 적 스프라이트는 아래(진행 방향)를 향하고, 아군은 위를 향한다
> - 바닥 그림자 색을 다르게 (적 = 붉은 기, 아군 = 푸른 기)
> - 체스판 자체가 흑백 격자이므로, **어두운 칸 위의 블랙 적**이 묻히지 않도록 적에게 밝은 외곽선(스트로크)을 넣는다

### 8.2 공격 이펙트

| 기물 | 속성 | 연출 방향 |
|---|---|---|
| **폰** | 노말 | 무채색~회백색 임팩트. 작은 원형 충격파 + 짧은 히트스톱. 0.5초마다 발생하므로 **가볍고 짧게** (0.1초 이내), 다수 배치 시에도 화면이 지저분해지지 않아야 함 |
| **룩** | **땅** | 갈색·황토색. 직선을 따라 **지면이 갈라지며 균열이 뻗어나가고 돌조각이 솟구침**. 묵직한 저주파 임팩트, 짧은 화면 진동 |
| **비숍** | **빛** | 흰색·금색. **얇고 선명한 광선**이 대각선을 관통, 렌즈 플레어와 잔광(0.3초 페이드). 룩과 정반대로 가볍고 예리한 느낌 |
| **나이트** | **불** | 주황·적색. 착지 지점에서 **방사형 폭발**, 잔불 파티클이 9칸에 흩어짐. 화면 흔들림 약간. 단발성이라 가장 화려해도 됨 |
| **퀸** | (버프) | 공격 없음. 8방향 라인을 따라 은은한 상시 오라 + 버프 대상 기물 발밑에 링 이펙트 |

> 체스판이 흑백 격자라 배경 대비가 강하다. **룩의 갈색은 어두운 칸에서, 비숍의 흰빛은 밝은 칸에서 묻힐 수 있으므로** 각 이펙트에 반대 톤의 외곽선을 넣을 것.

---

## 9. 밸런스 데이터

### 9.1 플레이어

| 항목 | 값 |
|---|---|
| 시작 체력 | **30** |
| 일반 적 도달 시 | **−1** / 보스 도달 시 **−5** |
| 체력 회복 | **없음** |
| 전 게임 누수 허용 | 일반 적 기준 **29마리** (전체 452마리 중 6.4%) |

### 9.2 핵심 지표 — "적 1마리가 받는 최대 피해" (정적 배치 기준)

| 기물 | 대상 | 체류 시간 | 타격 횟수 | 총 피해 |
|---|---|---:|---:|---:|
| 폰 | 일반 적 | 3초 | 6회 | **12** |
| 폰 | 보스 | 6초 | 12회 | **24** |
| 비숍 | 일반 적 (인접 파일) | 6초 | 2회 | **6** |
| 비숍 | 보스 | 12초 | 4회 | **12** |
| **룩 (자기 파일)** | 일반 적 | 24초 | 8회 | **40** |
| **룩 (자기 파일)** | 보스 | 48초 | 16회 | **80** |
| 룩 (타 파일) | 일반 적 | 3초 | 1회 | **5** |
| 나이트 | 전부 | 순간 | 1회 | **3** † |

† 나이트의 "1회 3"은 **한 번의 정적 배치**를 기준으로 한 값이며, 이동 쿨다운이 있던 v1.0 기준으로도
이미 이동을 반복하면 그만큼 반복 가능했다. v1.1에서 이동 쿨다운이 폐지되면서 그 반복 빈도의 상한
(구 3초)마저 사라져, 나이트의 실질 화력은 시간이 아니라 **입력 속도**로만 제한된다 — 9.4 참고.

### 9.3 웨이브별 요구 화력 (정적 배치 기준)

| 웨이브 | 적 체력 | 1킬 필요 조합 | 8파일 커버 비용 |
|---:|---:|---|---:|
| 1~3 | 10~12 | 폰 1개 (12) | 폰 4개 = 400G |
| 4~6 | 13~15 | 폰 1 + 비숍 1 (18) | 700G |
| 7~10 | 16~19 | 폰 2개 (24) | 800G |
| 11~13 | 22~28 | 폰 2 + 비숍 1 (30) | 1,100G |
| 14~16 | 31~37 | 룩 1개 (40) | 4,000G |
| 17~19 | 40~46 | 룩 1 + 폰 1 (52) | 4,800G |

누적 자금 곡선(4.5)과 대조하면 모든 구간에서 자금이 요구치를 앞선다. 실시간 재배치를 활용하면 이보다 적은 기물로도 방어가 가능하므로, 위 표는 **가장 소극적인 플레이 기준의 상한선**이다.

### 9.4 보스전 설계 — 폰 추격 전략 (의도된 동작)

기물 재배치에 제한이 없으므로, **보스를 폰으로 따라다니며 잡는 전략이 성립한다. 이는 버그가 아니라 의도된 동작이다.**

폰의 사거리는 전방 대각선 1칸이다. 보스가 한 칸 내려갈 때마다(6초) 폰을 함께 내려주면 보스는 48초 내내 폰의 사거리 안에 머문다.

| 기물 | 정적 배치 시 보스 피해 | **추격 시 보스 피해** | 가격 |
|---|---:|---:|---:|
| 폰 | 24 | **192** | 100G |
| 비숍 | 12 | 48 | 300G |
| 룩 (보스 파일) | 80 | 80 (추격 무의미) | 500G |
| 나이트 | 3 | **무효화됨 (v1.1) ‡** | 300G |

‡ 이 "48"(= 48초 완주 시간 ÷ 3초 이동 쿨다운 × 데미지 3)이라는 옛 계산은 v1.0의 3초 이동
쿨다운을 전제로 한 값이었다. v1.1에서 그 쿨다운이 폐지되면서 전제 자체가 사라졌으므로, 이 숫자를
새 쿨다운 값으로 재계산해 대체하지 않는다 — 재계산할 "새로운 상한"이 없기 때문이다. v1.1의
나이트는 플레이어가 드래그/클릭으로 얼마나 빨리 반복해서 옮길 수 있는지에 의해서만 화력이
제한되며, 이는 시간(초) 단위의 정적 수치로 표현할 수 있는 종류의 값이 아니다.

**보스별 클리어 난이도**

| 웨이브 | 보스 체력 | 추격 폰 필요 수 | 비용 | 직전 누적 자금 |
|---:|---:|---:|---:|---:|
| 5 | 420 | 3개 | 300G | 2,108G |
| 10 | 570 | 3개 | 300G | 5,556G |
| 15 | 1,020 | 6개 | 600G | 11,154G |
| 20 | 1,470 | 8개 | 800G | 20,842G |

**설계 의도 및 구현 시 유의점**
- 보스전은 **미세 조작(마이크로) 능력을 시험하는 구간**이 된다. 폰을 3~8개 동시에 6초마다 끌어 내려야 하므로, 실제 조작 난이도는 수치가 시사하는 것만큼 낮지 않다.
- 다만 **보스가 여러 파일에 걸쳐 있지 않으므로**, 추격에 성공하면 보스는 확정적으로 죽는다. 보스전 난이도는 조작 숙련도에 크게 좌우된다.
- 룩과 퀸은 **일반 웨이브의 주력**으로 남는다. 일반 웨이브는 적 수십 마리가 8개 파일에 동시다발로 흐르므로 추격 조작이 사실상 불가능하고, 후반 체력(37~46)을 넘기려면 룩(40) 또는 퀸 버프가 필수다. 따라서 상점 5종이 모두 존재 의의를 갖는다.
- **드래그 조작의 응답성이 곧 보스전 난이도**가 된다. 7.5의 클릭-투-무브 지원이 특히 중요하다.

### 9.5 플레이 테스트 시 확인할 항목
1. **보스전 조작 난이도** — 폰 추격이 "긴장감 있는 미세 조작"인지, "지루한 반복 드래그"인지. 후자라면 보스 체력을 낮추고 정적 화력 요구치를 높이는 방향으로 재조정
2. **일반 웨이브의 재배치 이득 폭** — 숙련자와 초심자의 난이도 편차
3. **후반(16~19웨이브) 난이도** — 체력 +3 스케일링이 충분한지
4. **웨이브 5 보스** — 첫 보스가 진입 장벽으로 작동하는지, 적절한 관문인지

---

## 10. 기술 사양

### 10.1 스택

| 항목 | 선택 |
|---|---|
| 언어 | **TypeScript** |
| 빌드 | **Vite** |
| 렌더링 | **HTML5 Canvas 2D** (보드·적·이펙트) |
| UI 레이어 | **DOM 오버레이** (슬롯·상점·HUD) |
| 상태 관리 | 순수 TS 클래스 |
| 백엔드 / 세이브 | **없음** |
| 사운드 | **공격 사운드만 있음 (v1.2)** — Web Audio API 직접 사용(외부 라이브러리 없음). 웨이브/보스/승리 등 다른 사운드는 여전히 없음 |

#### 10.1.1 공격 사운드 (v1.2 — 첫 슬라이스)

폰·비숍·룩의 `attack` 이벤트와 나이트의 `knightBlast` 이벤트에 맞춰 짧은 효과음을 재생한다.
그 외 이벤트(처치·누수·웨이브 전환·보스 등장·승패)는 이 슬라이스에서 소리를 내지 않는다.

| 큐 | 매핑 이벤트 | 에셋 |
|---|---|---|
| pawn | `attack` (pieceType=pawn) | `fire-pawn.ogg` |
| bishop | `attack` (pieceType=bishop) | `fire-bishop.ogg` |
| rook | `attack` (pieceType=rook) | `fire-rook.ogg` |
| knight | `knightBlast` | `blast-knight.ogg` |

늦은 웨이브에서는 폰 여러 개가 초당 최대 8회까지 발사할 수 있어(2배속이면 16회) 그대로 재생하면
소음이 된다. 세 가지 방어를 이 순서로 적용한다.

1. **프레임 내 코일레싱** — 한 프레임에 같은 큐가 여러 번 발생해도 재생은 1회
2. **큐별 최소 재생 간격(스로틀)** — 큐 종류마다 독립적으로 최소 간격을 둔다
3. **동시 재생 목소리 상한** — 넘으면 재생 중인 소리를 끊지 않고 최신 요청을 버린다

여기에 재생마다 피치(재생속도)를 소폭(±5% 안팎) 무작위로 흔들어 짧은 샘플의 기계적 반복감을
줄인다. 튜닝 수치(스로틀 간격·큐별 게인·목소리 상한·피치 변주 폭)는 코드 한 곳
(`src/audio/cues.ts`의 `AUDIO_TUNING`)에 모여 있으며, 플레이 후 값만 조정하면 된다 — 정확한
숫자는 그 표를 최신 기준으로 삼는다(문서에 중복 기재하지 않음).

브라우저 자동재생 정책상 사용자 제스처 이전에는 소리가 나지 않는다 — 이 게임은 드래그 기반이라
첫 `pointerdown`에서 오디오 컨텍스트를 준비한다. 에셋은 OGG(Vorbis)이며 Chrome·Firefox·Edge와
Safari 17.4+에서 재생된다(그 이전 Safari는 디코드 실패 시 해당 소리만 조용히 무음 처리).
출처·라이선스는 `NOTICE.md` 참조(Kenney, CC0).

### 10.2 게임 루프

```
requestAnimationFrame
  ↓
paused 이면 업데이트 건너뛰고 렌더만 수행
  ↓
고정 타임스텝 (dt = 1/60초 누적, dt × speedMultiplier)
  ├─ 준비 시간 카운트다운 (배속 적용)
  ├─ 적 스폰 타이머
  ├─ 적 위치 갱신 / 현재 칸 재계산
  ├─ 기물 쿨다운 감소 → 도달 시 공격 판정
  ├─ 데미지 적용 / 사망 처리 / 골드 지급
  ├─ 1랭크 통과 판정 → 체력 감소
  │    └─ 체력 0 이하 → phase='defeat', 즉시 정지
  └─ 웨이브 종료 판정 → 20웨이브 종료 시 phase='victory'
  ↓
렌더링 (배속과 무관하게 60fps)
```

### 10.3 설정 상수 (config.ts)

```typescript
export const CONFIG = {
  board: { files: 8, ranks: 8, squarePx: 80 },

  player: { startHp: 30, startGold: 300, hpLossNormal: 1, hpLossBoss: 5 },

  wave: {
    total: 20,
    prepareSeconds: 10,
    clearBonus: 300,
    spawnInterval: 1.0,
    countBase: 10,
    countPerWave: 2,        // 10 + 2*(w-1)
    bossEvery: 5,
  },

  enemy: {
    hpBase: 10,
    hpPerWaveEarly: 1,      // w <= 10
    hpPerWaveLate: 3,       // w >= 11
    hpScalingBreakpoint: 10,
    secondsPerSquare: 3.0,
    bossHpMultiplier: 30,
    bossSpeedMultiplier: 0.5,
    spritePx: 44,
    jitterPx: 8,
  },

  pieces: {
    pawn:   { cost: 100, damage: 2, interval: 0.5 },
    knight: { cost: 300, damage: 3, interval: 0   },  // 이동 쿨다운 없음 (v1.1, 사용자 요청)
    bishop: { cost: 300, damage: 3, interval: 3.0 },
    rook:   { cost: 500, damage: 5, interval: 3.0 },
    queen:  { cost: 900, damage: 0, interval: 0   },
  },

  economy: { sellRatio: 0.5 },
  slots: { rows: 4, cols: 4 },
} as const;

export function enemyHp(wave: number): number {
  const { hpBase, hpPerWaveEarly, hpPerWaveLate, hpScalingBreakpoint } = CONFIG.enemy;
  return wave <= hpScalingBreakpoint
    ? hpBase + (wave - 1) * hpPerWaveEarly
    : hpBase + (hpScalingBreakpoint - 1) * hpPerWaveEarly
      + (wave - hpScalingBreakpoint) * hpPerWaveLate;
}

export function enemyCount(wave: number): number {
  if (wave % CONFIG.wave.bossEvery === 0) return 1;   // 보스 단독
  return CONFIG.wave.countBase + CONFIG.wave.countPerWave * (wave - 1);
}
```

### 10.4 주요 데이터 구조

```typescript
type PieceType = 'pawn' | 'knight' | 'bishop' | 'rook' | 'queen';

interface Piece {
  id: string;              // 쿨다운은 이 ID 기준으로 유지된다
  type: PieceType;
  square: { file: number; rank: number } | null; // null = 슬롯
  slotIndex: number | null;
  cooldown: number;        // 이동/회수해도 초기화되지 않음
  queenBuffCount: number;
}
// 최종 공격력 = CONFIG.pieces[type].damage * (1 + queenBuffCount)

interface Enemy {
  id: string;
  file: number;       // 스폰 파일 고정
  y: number;          // 픽셀 단위 세로 위치 (연속값)
  hp: number;
  maxHp: number;      // = 처치 보상 골드
  isBoss: boolean;
  speed: number;      // px/s
  jitterX: number;    // 렌더 전용
}

interface GameState {
  hp: number;
  gold: number;
  wave: number;              // 1..20
  phase: 'prepare' | 'wave' | 'victory' | 'defeat';
  prepareTimer: number;
  spawnTimer: number;
  spawnedCount: number;
  speedMultiplier: 1 | 2;
  paused: boolean;
  pieces: Piece[];
  enemies: Enemy[];
  stats: { totalKills: number; totalGoldEarned: number };
}
```

### 10.5 구현 시 주의점
- **쿨다운은 반드시 기물 ID에 종속시킬 것.** 이동·회수·재배치 어느 경우에도 초기화되지 않아야 한다. 칸이나 슬롯 인덱스에 쿨다운을 저장하면 우회가 발생한다
- **재배치가 빈번하므로 퀸 버프 재계산 트리거를 빠짐없이 걸 것**: 배치 / 이동 / 회수 / 판매 / 나이트 이동 전부 포함. 단 매 프레임 계산은 불필요
- 비숍/룩의 관통 레이저는 차단 판정이 없으므로, 공격 시 해당 라인의 적을 필터링해 일괄 데미지
- 적 렌더링 지터는 표시 전용, 판정은 논리 좌표 기준
- 게임오버는 **체력 감소 처리 직후 즉시** phase 전환 (같은 프레임의 나머지 적 처리를 계속하지 않음)
- 20웨이브는 **보스 처치와 보스 누수 양쪽 모두 승리**로 처리 (누수 시 체력 −5를 먼저 적용하고, 체력이 0 이하면 패배가 우선)
- 모든 밸런스 수치는 `config.ts` 한 곳에서만 관리

---

## 11. 개발 우선순위

| 단계 | 내용 |
|---|---|
| **1차** | 보드 렌더링, 적(폰) 스폰/이동/1랭크 도달, 체력바, 플레이어 체력 |
| **2차** | 슬롯·상점·판매 슬롯, 드래그 앤 드롭 전체(**보드 내 재배치 포함**), 폰 배치 및 공격 |
| **3차** | 비숍·룩(관통 레이저), 나이트(드래그 이동 + 쿨다운 + 광역) |
| **4차** | 퀸 버프 시스템 및 중첩 계산 |
| **5차** | 웨이브 관리, 준비 시간, 보스(킹) + 경고 표시, 승리/게임오버 및 결과 화면 |
| **6차** | 사거리 미리보기, 버프 시각화, 툴팁, 일시정지/배속, 클릭-투-무브 |
| **7차** | 이펙트 연출 (땅/빛/불/노말), 진영 색상 구분 |
| **8차** | 플레이 테스트 및 밸런싱 (9.5 참조) |

---

**문서 버전:** v1.2
**최초 확정일:** 2026-08-08 (v1.0)
**최종 수정일:** 2026-08-14 (v1.2)

**변경 이력**
- v0.1: 최초 구두 기획 정리
- v0.2: 적/웨이브/기물 수치, 경제, UI 동작, 기술 스택 확정
- v0.3: 총 웨이브·보스·편의 기능 확정, 밸런스 시뮬레이션으로 웨이브 1 클리어 불가 문제 발견
- v0.4: 조정안 A(적 수 감소)·B(체력 30)·C(폰 데미지 2)·D(이동 속도 3초/칸) 적용 및 전체 수치 재계산
- v0.5: 실시간 재배치를 핵심 메커니즘으로 확정, 후반 체력 증가폭 +3 적용, 아트/이펙트 방향 추가
- v1.0: 보스전 폰 추격을 의도된 설계로 확정, 쿨다운 ID 기준 유지 규칙, 종료 조건 확정, 진영 색상 및 보스 경고 표시 추가. 전 항목 확정 완료
- **v1.1 (2026-08-14): v1.0 구현이 끝난 뒤 사용자가 요청하고 밸런스 결과를 승인한 두 가지 게임
  규칙 변경을 반영.**
  **(1) 나이트 이동 쿨다운 폐지** — `CONFIG.pieces.knight.interval`을 3초 → 0으로 낮춰, 나이트가
  배치·이동마다 매번 폭발하도록 변경(5.3). 쿨다운 자체가 사라지면서 5.1의 "회수→재배치로 쿨다운
  우회 불가" 안티파밍 조항은 나이트에 한해 **의도적으로 폐기**됐다 — 우회할 쿨다운이 이제 없기
  때문이다. 쿨다운 게이트 메커니즘(코드)은 그대로 남아 있어 config 값만 되돌리면 v1.0 동작이
  복원된다. 9.2/9.4의 나이트 관련 정적 화력 수치 중 옛 3초 쿨다운을 전제로 계산된 것은 새 값으로
  재계산하지 않고 **무효화됨**으로 표기했다 — 나이트의 화력이 이제 시간이 아니라 입력 속도로만
  제한되기 때문이다.
  **(2) 점유 칸 맞교환** — 보드 위 기물을 이미 다른 기물이 점유한 칸으로 이동시키면(트레이 → 보드
  배치는 제외) 실패해 원위치로 돌아가는 대신, 두 기물이 서로 자리를 맞바꾼다(5.1, 5.3, 7.5). 양쪽
  기물의 쿨다운은 각자 그대로 유지되고, 퀸 버프는 맞교환 직후 한 번 재계산되며, 나이트가 직접
  옮긴 쪽만 새 자리에서 폭발한다(맞교환으로 밀려난 기물은 나이트여도 폭발하지 않는다).
- **v1.2 (2026-08-14): 공격 사운드 도입 — 첫 슬라이스.** 사용자가 실제 플레이로 들어보고 다른
  사운드(UI 클릭, 웨이브/보스/승리) 여부를 판단하기 전 단계로, **공격 사운드만** 우선 추가했다
  (10.1/10.1.1, 7.6).
  **(1) 4개 큐** — 폰/비숍/룩의 `attack`과 나이트의 `knightBlast`에 각각 발사음·블라스트음을
  매핑했다(10.1.1 표). 그 외 이벤트(처치·누수·웨이브 전환·보스 등장·승패)는 여전히 무음이다.
  **(2) 소음 방지 3단 방어** — 늦은 웨이브에서 폰이 초당 최대 8회(2배속 16회) 발사해도 그대로
  재생하면 소음이 되는 문제를, 프레임 내 코일레싱 → 큐별 최소 재생 간격(스로틀) → 동시 재생
  목소리 상한 순으로 방어했다. 상한을 넘으면 재생 중인 소리를 끊지 않고 최신 요청만 버린다.
  재생마다 피치를 소폭 무작위로 흔들어(디튠) 기계적 반복감도 줄였다. 모든 튜닝 수치는
  `src/audio/cues.ts`의 `AUDIO_TUNING` 표 하나에 모아 뒀다(7.6/10.1.1).
  **(3) 음소거 토글** — HUD에 일시정지·배속 버튼 옆으로 추가했다(7.6). 세이브 시스템이 없으므로
  메모리 상태로만 유지하고 영속화하지 않는다.
  **(4) 자동재생 정책 대응** — 브라우저는 사용자 제스처 전에는 소리를 내지 않는다. 드래그 기반
  게임의 특성을 살려 첫 `pointerdown`에서 오디오 컨텍스트를 준비한다.
  **(5) 에셋** — Kenney(CC0)에서 가져온 OGG(Vorbis) 4개. 출처·검토 후보·기각 사유는
  `NOTICE.md` 참조.
