# 서드파티 에셋 고지 (Third-party asset notice)

## 체스 기물 SVG — `src/assets/pieces/`

이 저장소의 체스 기물 이미지는 직접 제작한 것이 아니라 위키미디어 공용(Wikimedia Commons)의
표준 체스 기물 세트(통칭 **Cburnett 세트**)를 가져온 것이다. `react-chessboard` 패키지가
기본 기물로 쓰는 것과 같은 아트워크다.

| 항목 | 내용 |
|---|---|
| 저작자 | Colin M.L. Burnett (Wikimedia 사용자 [Cburnett](https://commons.wikimedia.org/wiki/User:Cburnett)) |
| 출처 | [Wikimedia Commons — Category:SVG chess pieces](https://commons.wikimedia.org/wiki/Category:SVG_chess_pieces) |
| 라이선스 | [CC BY-SA 3.0](https://creativecommons.org/licenses/by-sa/3.0/) (원본은 GFDL·BSD로도 다중 라이선스됨) |
| 변경 여부 | **있음** — 아래 "변경 내역" 참조 |

### 파일 대응표

저장소에서는 게임 내 역할이 드러나는 이름으로 바꿔 두었다. 원본 파일명은 다음과 같다.

| 저장소 경로 | 위키미디어 원본 파일 | 게임 내 역할 |
|---|---|---|
| `src/assets/pieces/ally-pawn.svg` | `Chess_plt45.svg` | 플레이어 폰 (화이트) |
| `src/assets/pieces/ally-knight.svg` | `Chess_nlt45.svg` | 플레이어 나이트 (화이트) |
| `src/assets/pieces/ally-bishop.svg` | `Chess_blt45.svg` | 플레이어 비숍 (화이트) |
| `src/assets/pieces/ally-rook.svg` | `Chess_rlt45.svg` | 플레이어 룩 (화이트) |
| `src/assets/pieces/ally-queen.svg` | `Chess_qlt45.svg` | 플레이어 퀸 (화이트) |
| `src/assets/pieces/enemy-pawn.svg` | `Chess_pdt45.svg` | 일반 적 (블랙) |
| `src/assets/pieces/enemy-king.svg` | `Chess_kdt45.svg` | 보스 (블랙) |

각 원본은 `https://commons.wikimedia.org/wiki/File:<원본 파일명>` 에서 확인할 수 있다.

### 페어리 기물 3종 (v1.9 도입 → ★ v1.10에서 아트워크 교체)

게임에 페어리 체스 기물(아치비숍·챈슬러·아마존)이 추가되면서 아트워크가 필요해졌다.

**v1.9에서는 직접 합성해 썼다.** 위키미디어 표준 세트에 이 세 기물의 Cburnett 판본이 없다고
판단하고, 저장소가 이미 갖고 있는 SVG를 조합해 만들었다 — 재료 기물(비숍·룩·퀸)을 0.86배로
줄여 왼쪽 위에 두고 나이트를 0.44배로 줄여 오른쪽 아래에 흰 원반과 함께 얹는 방식이었다.

**★ v1.10에서 위키미디어의 실제 페어리 기물로 교체했다.** 그 판단은 틀렸었다 — 공용에는
[Category:SVG chess pieces/Fairy](https://commons.wikimedia.org/wiki/Category:SVG_chess_pieces)에
같은 계열의 페어리 기물이 실제로 존재한다. 교체한 이유는 아트워크 품질이다: 직접 만든 합성물은
재료 둘이 **분리된 두 덩어리**(본체 + 모서리 배지)로 읽혀 배지가 칸 경계 밖으로 삐져나갔지만,
공용 판본은 **하나의 실루엣**이다 — 비숍의 물결 받침 위에 말 머리(아치비숍), 룩의 성벽 받침
위에 말 머리(챈슬러), 퀸의 왕관을 쓴 말 머리(아마존). 기본 5종과 선 두께·조형 어휘도 일치한다.

| 저장소 경로 | 위키미디어 원본 파일 | 저작자 | 라이선스 |
|---|---|---|---|
| `src/assets/pieces/ally-archbishop.svg` | `Chess_alt45.svg` | NikNaks93 (파생) — 원저작 `Chess_blt45.svg`·`Chess_nlt45.svg` by Cburnett, `Chess_all44.png` by OmegaChessFan | CC BY-SA 3.0 |
| `src/assets/pieces/ally-chancellor.svg` | `Chess_clt45.svg` | NikNaks93 (파생) — 원저작 `Chess_alt45.svg`(위), `Chess_cld44.png` by OmegaChessFan | CC BY-SA 3.0 |
| `src/assets/pieces/ally-amazon.svg` | `Chess_Alt45.svg` | Mszulc29 (자작으로 표기) | **CC BY-SA 4.0** |

⚠️ **아마존만 라이선스 버전이 다르다.** 나머지 아홉 파일은 전부 CC BY-SA 3.0인데
`Chess_Alt45.svg`는 2024년에 업로드된 CC BY-SA **4.0** 저작물이다. 두 버전은 별개의 라이선스
문서이므로 크레딧 줄에 "CC BY-SA 3.0"만 적으면 이 파일의 저작자 표시 의무가 이행되지 않는다 —
그래서 `src/ui/layout.ts`의 `CREDIT_HTML`이 **두 라이선스를 모두** 링크한다.
(`tests/ui.test.ts`가 저작자 셋과 라이선스 둘의 표시를 단언한다 — 라이선스 위반은 테스트가
저절로 실패하지 않는 종류의 결함이라, 알아채기 전까지 배포된 채로 남기 때문이다.)

⚠️ **원본 파일명의 대소문자가 의미를 갖는다.** `Chess_alt45.svg`(소문자 a)는 아치비숍이고
`Chess_Alt45.svg`(대문자 A)는 아마존으로, **완전히 다른 파일**이다. macOS처럼 대소문자를
구분하지 않는 파일시스템에서 원본 이름 그대로 두 파일을 같은 디렉터리에 내려받으면 한쪽이
다른 쪽을 덮어쓴다 (이 작업 중 실제로 겪었다). 저장소에는 역할 이름으로 저장하므로 문제가
없지만, 원본을 다시 받을 일이 있으면 주의할 것.

### 변경 내역

원본 파일 **10개**(기본 5 + 적 2 + 페어리 3)는 다음 세 가지만 달라졌으며, **경로 데이터·색상·
형태는 전혀 손대지 않았다.** 셋 다 파일마다 필요할 때만 적용했다.

1. **`viewBox="0 0 45 45"` 속성 추가.** 원본은 `width="45" height="45"`만 있고 `viewBox`가 없어
   임의 크기로 확대할 때 브라우저마다 래스터화 결과가 달라진다. 게임은 캔버스에 44~72px로 그리므로
   좌표계를 명시할 필요가 있었다. v1.10에 추가된 셋 중 `Chess_clt45.svg`(챈슬러)와
   `Chess_Alt45.svg`(아마존)에 적용했고, `Chess_alt45.svg`(아치비숍)는 원본에 이미 있어 그대로 뒀다.
2. **줄바꿈 문자 정규화 (CRLF → LF).** 내려받을 때 일부 파일이 CRLF로 전송되어 저장소 규약에 맞춰
   LF로 통일했다.
3. **파일 끝 개행 추가.** 저장소 규약에 맞춘 것으로, 아치비숍 한 파일에만 해당한다.

바이트 단위 비교로 이 셋 외에는 원본과 동일함을 확인했다. v1.10 도입분 3종의 원본 SHA-256
앞 16자리는 각각 `f709acbbf766aaa6`(아치비숍) · `63317e5e22c943f7`(챈슬러) ·
`58adbb9e703b85ef`(아마존)이다 — 원본을 다시 받아 대조할 때 쓸 수 있다.

### CC BY-SA 이행 사항 (3.0 · 4.0 공통)

- **저작자 표시(BY)**: 위 표의 저작자·출처·라이선스 링크가 그 이행이다. 게임 화면에도
  `#main`(보드·HUD·상점) 바로 아래 상시 노출되는 `<footer id="credit">`로 같은 크레딧을
  노출한다 — 결과 화면에만 잠깐 뜨는 배너가 아니라 플레이 내내(준비/웨이브/결과 어느 단계든)
  항상 DOM에 있는 요소다. **시작 화면(v1.5, 스펙 7.10)도 같은 기물 SVG를 보여주므로 같은
  크레딧 줄을 함께 띄운다** — 두 화면이 `src/ui/layout.ts`의 `CREDIT_HTML` 상수 하나를 공유해,
  한쪽 문구만 갈라지거나 한 화면에서 표시가 누락되는 일이 생기지 않는다. 이 크레딧 줄은
  저작자·출처 링크, 라이선스 링크에 더해
  이 문서(`NOTICE.md`)로 가는 링크도 포함한다 — `NOTICE.md`는 `dist/`에 포함되지 않으므로
  배포된 사이트 방문자가 변경 내역까지 확인하려면 저장소 상의 이 파일로 가는 경로가 필요하다.
  ★ v1.10부터 이 줄은 저작자 셋(Cburnett · NikNaks93 · Mszulc29)과 **라이선스 두 버전**을
  모두 링크한다. 아마존 한 파일이 CC BY-SA 4.0이기 때문이다(위 ⚠️ 참조).
- **동일조건변경허락(SA)**: 위 "변경 내역"의 수정본은 **각자의 원본과 같은 라이선스**로 배포한다 —
  CC BY-SA 3.0 원본에서 온 아홉 파일은 3.0으로, CC BY-SA 4.0인 아마존은 4.0으로. 이 조항은
  **기물 이미지 에셋에만 적용되며**, 이 저장소의 소스 코드에는 미치지 않는다.

## 보드 색상

밝은 칸 `#F0D9B5` / 어두운 칸 `#B58863`. `react-chessboard`의 기본값
(`customLightSquareStyle` / `customDarkSquareStyle`)과 같은 값으로, 리체스·chess.com 계열에서
널리 쓰이는 관용적인 색 조합이다. 색상값 자체는 저작물이 아니므로 별도 라이선스 의무는 없다.

## 효과음 — `src/assets/sounds/`

★ **v1.10에서 `blast-knight.ogg`를 삭제해 현재 14개다.** 나이트의 능력이 3×3 폭발에서 지속형
감속 오라로 바뀌면서 그 큐가 울릴 순간 자체가 없어졌다 — 감속은 사건이 아니라 상태이고, 웨이브당
수십 번 발생하는 진입 전이에 소리를 붙이면 그대로 잡음이 된다. 게인 0으로 죽이지 않고 큐 종류
(`CueKind`)·`CUE_URL` 항목·에셋 파일까지 함께 지웠다(v1.4의 `uiPickup` 제거와 같은 방식).

체스 디펜스 v1.2에서 **공격 사운드 전용** 첫 슬라이스(4개)로 시작해, v1.3에서 나머지 전체
효과음 세트(12개, 처치/누수/보스경고/웨이브클리어/승패/UI 제스처)를 추가했다. v1.4에서는
사용자가 실제로 들어본 뒤 UI 제스처 사운드를 하나 정리했다 — 집기/선택 시작(`ui-pickup.ogg`)을
완전히 제거하고, 그 자리에 쓰던 짧은 틱음을 배치/이동/회수 성공(`ui-place.ogg`)이 대신
물려받았다(기존 배치 성공음은 폐기). v1.4 시점에는 15개였고, v1.10에서 14개가 됐다
(`chess-defense-game-spec.md` §10.1/§7.6 참조). 전부 [Kenney](https://kenney.nl/)가 제작해
공개한 SFX 팩에서 가져왔다.

| 항목 | 내용 |
|---|---|
| 제작자 | [Kenney](https://kenney.nl/) |
| 출처 | [kenney.nl/assets](https://kenney.nl/assets) (Interface Sounds / Impact Sounds 계열 팩) |
| 라이선스 | [CC0 1.0](https://creativecommons.org/publicdomain/zero/1.0/) — 퍼블릭 도메인에 준함, 저작자 표시 불필요 |
| 변경 여부 | **없음** — 원본 오디오 데이터를 그대로 사용 |

라이선스상 크레딧 표시가 필수는 아니지만, 다른 에셋과 동일한 방식으로 여기 기록해 둔다.

### 파일 대응표 및 원본 파일명 추적

저장소에서는 게임 내 역할이 드러나는 이름으로 바꿔 두었다. 14개 파일 모두 Ogg Vorbis 코멘트
헤더에 `ARTIST=KenneyG` / `COMMENTS=Sound generated by GameSynth from Tsugi (www.tsugi-studio.com)`
메타데이터가 그대로 남아 있어 Kenney 제작·GameSynth 생성이라는 점은 파일 자체로 확인된다.
다만 이 헤더에는 **원본 파일명 필드가 없어**, 대부분의 파일은 Kenney의 어느 팩·어느 원본
파일명에 대응하는지 이 저장소 안의 정보만으로 다시 추적할 수 없다 — 정확하지 않은 원본
파일명을 지어내 적어두는 대신 이 사실을 그대로 기록해 둔다. `ui-place.ogg`(v1.4 교체분)만은
예외로, 원본 파일명(`select_001.ogg`)이 별도로 확인됐다(아래 표).

| 저장소 경로 | 게임 내 역할 | 실측 길이 | 원본 Kenney 파일명 |
|---|---|---|---|
| `src/assets/sounds/fire-pawn.ogg` | 폰 `attack` 큐 | 0.097s | 불명 (Vorbis 코멘트에 파일명 필드 없음) |
| `src/assets/sounds/fire-bishop.ogg` | 비숍 `attack` 큐 | — | 불명 (Vorbis 코멘트에 파일명 필드 없음) |
| `src/assets/sounds/fire-rook.ogg` | 룩 `attack` 큐 | — | 불명 (Vorbis 코멘트에 파일명 필드 없음) |
| `src/assets/sounds/enemy-die.ogg` | 일반 적 처치(`enemyDied`, `isBoss:false`) | 0.159s | 불명 (Vorbis 코멘트에 파일명 필드 없음) |
| `src/assets/sounds/boss-die.ogg` | 보스 처치(`enemyDied`, `isBoss:true`) | 0.352s | 불명 (Vorbis 코멘트에 파일명 필드 없음) |
| `src/assets/sounds/enemy-leak.ogg` | 적 누수(`enemyLeaked`, 일반/보스 공통) | 0.165s | 불명 (Vorbis 코멘트에 파일명 필드 없음) |
| `src/assets/sounds/boss-spawn.ogg` | 보스 경고(`bossSpawned` + `prepareStarted{isBossWave:true}`) | 1.16s | 불명 — **추정 배정**(아래 참고) |
| `src/assets/sounds/wave-clear.ogg` | 웨이브 클리어(`waveCleared`) | 0.49s | 불명 — **추정 배정** |
| `src/assets/sounds/victory.ogg` | 승리(phase → `'victory'`) | 1.39s | 불명 — **추정 배정** |
| `src/assets/sounds/defeat.ogg` | 패배(phase → `'defeat'`) | 1.05s | 불명 — **추정 배정** |
| `src/assets/sounds/ui-buy.ogg` | 상점 구매 성공 | 0.290s | 불명 (Vorbis 코멘트에 파일명 필드 없음) |
| `src/assets/sounds/ui-sell.ogg` | 판매 성공 | 0.314s | 불명 (Vorbis 코멘트에 파일명 필드 없음) |
| `src/assets/sounds/ui-place.ogg` | 배치/이동/회수 성공 (**v1.4 교체**) | 0.043s | `select_001.ogg` — 사용자 확인 (아래 참고) |
| `src/assets/sounds/ui-invalid.ogg` | 드롭/클릭-이동 거부 | 0.103s | 불명 (Vorbis 코멘트에 파일명 필드 없음) |

**`ui-place.ogg` 변경 이력 (v1.4).** v1.3 도입 당시 `ui-place.ogg`는 별도의 배치 성공음
(Kenney `drop_002.ogg`, 실측 0.191s)이었고, 집기/선택 시작에는 `ui-pickup.ogg`
(Kenney `select_001.ogg`, 실측 0.043s)를 따로 썼다. 사용자가 실제로 들어보고 (1) 집기/선택
시작은 무음이 낫다고 판단해 그 큐 자체를 제거하고, (2) 배치 성공음은 원래 집기음으로 쓰던 더
짧은 `select_001.ogg` 쪽이 낫다고 판단해 그쪽으로 교체했다. 그 결과 `drop_002.ogg`는 완전히
폐기됐고(파일 삭제), `select_001.ogg`는 이제 `ui-place.ogg`라는 이름 아래 남아 있다 — 파일
경로가 가리키는 게임 내 역할(배치 성공)과 실제 내용(원래 집기용으로 골랐던 샘플)이 이제
일치한다.

**"추정 배정" 4곳(`boss-spawn`/`wave-clear`/`victory`/`defeat`)은 실제로 들어보지 못한 채
이름만으로 배정한 것이다** — 8비트풍이면 승리/패배, 짧은 피치카토면 소규모 성공(웨이브클리어),
스팅어면 보스 경고일 것이라는 추정에 근거했다. `src/audio/player.ts`의 `CUE_URL` 표 하나가
큐↔파일의 유일한 대응 지점이므로, 실제로 들어보고 배정을 바꾸고 싶으면 그 표의 값 두 줄만
맞바꾸면 된다(코드 로직 변경 불필요).

반면 아래 "검토했지만 채택하지 않은 후보"의 원본 파일명은 선정 검토 당시 자료에 남아 있어
정확히 기록할 수 있다.

### 검토했지만 채택하지 않은 후보

같은 팩에서 함께 검토했으나, 아래 사유로 채택하지 않았다 (실측 재생 길이 포함).

| 후보 파일 | 실측 길이 | 기각 사유 |
|---|---|---|
| `click_003.ogg` | 0.010s | 너무 짧아 재생돼도 체감상 인지되지 않을 가능성이 높음 |
| `impactGeneric_light_000.ogg` | 0.139s | 폰 예산(0.1s대) 초과 — 폰의 재생 스로틀은 120ms인데 샘플이 그보다 길면 같은 소리가 자기 자신의 꼬리와 겹치기 시작함(채택된 `fire-pawn.ogg`는 0.097s로 스로틀보다 짧아 이 문제가 없음) |
| `impactBell_heavy_000.ogg` | 1.480s | 재생 길이·음색 모두 "블라스트"라는 캐릭터와 맞지 않음(종소리 계열) |
| `impactMining_000.ogg` | 0.937s | 나이트 블라스트 대비 과도하게 길어 스로틀(200ms)과 어울리지 않음 (⚠️ 이 후보를 평가한 기준이던 나이트 블라스트 자체가 v1.10에서 사라졌다) |

### OGG 포맷과 브라우저 호환

Kenney가 배포하는 원본 포맷이 OGG뿐이고 이 환경에는 ffmpeg 등 트랜스코딩 도구가 없어, 다른
포맷으로 변환하지 않고 OGG를 그대로 썼다. Chrome·Firefox·Edge와 Safari 17.4+는 `<audio>`/Web
Audio 양쪽에서 OGG(Vorbis)를 디코드한다. **Safari 17.4 미만에서는 OGG 디코드가 실패할 수 있다** —
`src/audio/player.ts`는 디코드 실패 시 예외를 던지지 않고 콘솔에 한 번만 로그한 뒤 해당 큐를
무음으로 남기므로(스프라이트 로더의 `img.onerror` 처리와 동일한 원칙), 구형 Safari에서도 게임
자체가 깨지지는 않지만 공격 사운드만 들리지 않는다.

## 기물 스킨 — `src/assets/pieces/*-skin-*.png` (v1.19)

기본 아트워크(위쪽 표)와 별개로, 플레이어가 시작 화면에서 골라 바꿀 수 있는 **스킨** 이미지다.
게임 규칙에는 아무 영향이 없고 그림만 바뀐다(`src/render/skins.ts`가 대응표를 소유한다).

| 파일 | 대상 기물 | 스킨 id | 크기 |
|---|---|---|---|
| `src/assets/pieces/pawn-skin-1.png` | 폰 | `heart-princess` | 288×288 RGBA (가공본) |
| `src/assets/pieces/pawn-skin-1.source.png` | — | — | 1254×1254 RGB (미가공 원본) |

`*.source.png`는 **코드에서 import하지 않는다** — Vite는 import된 것만 번들에 넣으므로 배포에
포함되지 않는다. 가공을 다시 하거나 다른 크기로 뽑을 때 필요한 원본을 잃지 않으려고 남겨 둔
것이며, 필요 없으면 지워도 게임은 그대로 동작한다.

### 출처와 이용 조건

| 항목 | 내용 |
|---|---|
| 생성 | 저장소 소유자가 **OpenAI GPT의 이미지 생성**으로 만들었다 |
| 가공 | 저장소 소유자(배경 제거·크롭·축소 — 아래 "변경 내역" 참조) |
| 제3자 저작자 | **없다** — 위키미디어 세트에서 온 것이 아니다 |
| 표시 의무 | **없다** (아래 근거) |

**왜 크레딧 줄에 넣지 않는가 — 근거 둘.**

1. **준수할 제3자 라이선스가 없다.** OpenAI 이용약관은 산출물(Output)에 대한 자사의 권리를
   이용자에게 양도하며, 저작자 표시를 요구하지 않는다. 기본 기물 SVG처럼 "CC BY-SA를 지키기
   위해" 화면에 띄워야 하는 대상이 아니다.
2. **표시할 저작자 자체가 성립하지 않는다.** 사람의 창작적 기여 없이 생성형 AI만으로 나온
   산출물은 한국·미국 모두 저작물로 인정되지 않는다는 것이 저작권 당국의 현재 입장이다
   (한국저작권위원회 AI 저작권 안내서, 미국 저작권청 2023년 등록 지침). 즉 이 파일에는
   표시할 저작자가 없고, 있지도 않은 저작자를 적는 쪽이 오히려 부정확하다.

⚠️ **그래서 이 파일은 CC BY-SA의 SA(동일조건변경허락) 대상도 아니다.** 위쪽 기물 SVG와 이
파일은 **법적 지위가 서로 다르다** — 한 디렉터리에 나란히 있다고 해서 같은 조건이 걸린다고
읽으면 안 된다.

⚠️ **`CREDIT_HTML`의 문구를 "기본 기물 이미지"로 좁힌 이유가 이것이다.** 플레이어가 스킨을
켜면 화면의 폰은 위키미디어 저작물이 아닌데, 크레딧이 "기물 이미지: Cburnett …"이라고만 적혀
있으면 **그들이 만들지 않은 그림을 그들의 것으로 표시하는** 셈이 된다. BY 조항은 저작자를
빠뜨리지 않는 것만이 아니라 **엉뚱한 사람을 적지 않는 것**이기도 하다.

> 법률 자문이 아니다. 위 판단은 공개된 약관과 저작권 당국의 안내를 근거로 한 것이며, 배포
> 형태나 관할이 달라지면 다시 볼 문제다.

### 변경 내역 — 폰 스킨 가공 (v1.19)

원본은 **알파 채널이 없는 RGB**였다(배경이 투명이 아니라 크림색 `#FEF9F5`). 그대로 쓰면 보드
위에 **흰 사각형째로** 그려진다 — 기물이 아니라 스티커가 얹힌 것처럼 보인다. 다음 순서로
가공했다.

1. **배경 제거** — 이미지 테두리에서 시작하는 flood fill로 "밝고 채도 낮은" 영역을 걷어냈다.
   캐릭터의 흰색(모자·받침)도 색만 보면 같지만, 기물 실루엣을 두르는 **짙은 외곽선이 fill을
   막아** 안쪽은 살아남는다. 색 문턱만으로 잘랐다면 흰 부분이 함께 지워졌을 것이다.
2. **경계 부드럽게 + 배경색 되돌리기** — 경계 픽셀은 배경과 외곽선이 섞인 값이라 이진 마스크로
   자르면 창백한 테두리가 남는다. 배경색과의 거리로 부분 알파를 주고 색에서 배경 성분을 다시
   뺐다(언블렌딩).
3. **크롭 + 정사각 패딩** — 기물 높이가 상자의 **80%**가 되도록 맞췄다. 기본 폰 SVG(65%)보다
   조금 크지만, 티어 고리(반지름 38px)를 가리지 않는 범위다.
4. **288×288로 축소** — 프리멀티플라이 상태에서 Lanczos로 줄여 투명 픽셀의 색이 가장자리로
   번지지 않게 했다. **1,202KB → 63KB**(게임은 72px로 구워 쓰므로 288px면 2× DPR까지 여유가 있다).


⚠️ **이 파일의 저작자·출처·라이선스는 아직 기록되지 않았다.** 저장소 소유자가 직접 추가한
이미지이며, 위키미디어 세트에서 온 것이 아니다. 제3자 저작물이거나 생성형 도구의 산출물이라면
그 사실과 이용 조건을 여기에 적어야 하고, 조건에 따라 `src/ui/layout.ts`의 `CREDIT_HTML`에도
줄이 하나 늘어야 한다 — 기본 기물 SVG의 크레딧은 위키미디어 세트만 가리키므로 스킨을 덮지
않는다. **저장소 소유자가 직접 만든 것이라면 그 사실을 여기 한 줄로 적어 두면 끝난다.**

## 그 밖의 에셋

`assets/` 디렉터리의 PNG 이미지는 저장소 소유자가 별도로 제공한 것으로, 현재 게임에서 사용하지
않으며 git에도 포함되어 있지 않다.
